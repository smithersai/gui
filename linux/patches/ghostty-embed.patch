diff --git a/build.zig b/build.zig
index b89958e55..e6dda4264 100644
--- a/build.zig
+++ b/build.zig
@@ -176,6 +176,11 @@ pub fn build(b: *std.Build) !void {
             resources.install();
             if (i18n) |v| v.install();
         }
+        if (config.app_runtime == .gtk and !config.emit_exe) {
+            const lib_gtk = try buildpkg.GhosttyLibGtk.initShared(b, &config, &deps);
+            lib_gtk.install();
+            resources.install();
+        }
     } else if (!config.emit_lib_vt) {
         // The macOS Ghostty Library
         //
diff --git a/src/apprt.zig b/src/apprt.zig
index c467f1801..1fd24ed9e 100644
--- a/src/apprt.zig
+++ b/src/apprt.zig
@@ -44,7 +44,10 @@ pub const runtime = switch (build_config.artifact) {
         .none => none,
         .gtk => gtk,
     },
-    .lib => embedded,
+    .lib => switch (build_config.app_runtime) {
+        .gtk => gtk,
+        else => embedded,
+    },
     .wasm_module => browser,
 };
 
diff --git a/src/apprt/gtk/App.zig b/src/apprt/gtk/App.zig
index 39c13c19d..1b7097638 100644
--- a/src/apprt/gtk/App.zig
+++ b/src/apprt/gtk/App.zig
@@ -91,3 +91,12 @@ pub fn performIpc(
 pub fn redrawInspector(_: *App, surface: *Surface) void {
     surface.redrawInspector();
 }
+
+pub fn keyboardLayout(_: *App) struct {
+    pub const OptionAsAlt = @import("../../input.zig").OptionAsAlt;
+    pub fn detectOptionAsAlt(_: @This()) OptionAsAlt {
+        return .false;
+    }
+} {
+    return .{};
+}
diff --git a/src/apprt/gtk/class/application.zig b/src/apprt/gtk/class/application.zig
index 873674cec..51a12eb99 100644
--- a/src/apprt/gtk/class/application.zig
+++ b/src/apprt/gtk/class/application.zig
@@ -221,6 +221,8 @@ pub const Application = extern struct {
         pub var offset: c_int = 0;
     };
 
+    var embedded_default: ?*Self = null;
+
     /// Get this application as the default, allowing access to its
     /// properties globally.
     ///
@@ -228,10 +230,19 @@ pub const Application = extern struct {
     /// default application is a GhosttyApplication. The program would have
     /// to be in a very bad state for this to be violated.
     pub fn default() *Self {
+        if (embedded_default) |app| return app;
         const app = gio.Application.getDefault().?;
         return gobject.ext.cast(Self, app).?;
     }
 
+    /// Set an embedder-owned default application.
+    ///
+    /// This lets a host GTK application embed Ghostty GTK surfaces without
+    /// making GhosttyApplication the process-wide GApplication singleton.
+    pub fn setEmbeddedDefault(app: ?*Self) void {
+        embedded_default = app;
+    }
+
     /// Creates a new Application instance.
     ///
     /// This does a lot more work than a typical class instantiation,
diff --git a/src/apprt/gtk/class/surface.zig b/src/apprt/gtk/class/surface.zig
index 179c779d7..94ac0846d 100644
--- a/src/apprt/gtk/class/surface.zig
+++ b/src/apprt/gtk/class/surface.zig
@@ -18,6 +18,7 @@ const internal_os = @import("../../../os/main.zig");
 const renderer = @import("../../../renderer.zig");
 const terminal = @import("../../../terminal/main.zig");
 const CoreSurface = @import("../../../Surface.zig");
+const CoreApp = @import("../../../App.zig");
 const gresource = @import("../build/gresource.zig");
 const ext = @import("../ext.zig");
 const gsettings = @import("../gsettings.zig");
@@ -716,18 +717,48 @@ pub const Surface = extern struct {
         pub var offset: c_int = 0;
     };
 
-    pub fn new(overrides: struct {
+    pub const NewOptions = struct {
         command: ?configpkg.Command = null,
         working_directory: ?[:0]const u8 = null,
         title: ?[:0]const u8 = null,
 
         pub const none: @This() = .{};
-    }) *Self {
+    };
+
+    var pending_embed_config: ?*Config = null;
+
+    pub fn new(overrides: NewOptions) *Self {
+        const app = Application.default();
+        const config = app.getConfig();
+        defer config.unref();
+        return newWithCoreAppAndOptions(app.core(), config, overrides);
+    }
+
+    /// Create a surface using an embedder-provided core Ghostty app.
+    ///
+    /// Host applications can use this to instantiate the GTK surface widget
+    /// without relying on GhosttyApplication being the process default
+    /// GApplication. Callers must also install an embedded default
+    /// Application with Application.setEmbeddedDefault so later surface
+    /// callbacks resolve the same core/runtime app.
+    pub fn newWithCoreApp(core_app: *CoreApp, config: *Config) *Self {
+        return newWithCoreAppAndOptions(core_app, config, .none);
+    }
+
+    fn newWithCoreAppAndOptions(
+        core_app: *CoreApp,
+        config: *Config,
+        overrides: NewOptions,
+    ) *Self {
+        const previous_config = pending_embed_config;
+        pending_embed_config = config;
+        defer pending_embed_config = previous_config;
+
         const self = gobject.ext.newInstance(Self, .{
             .@"title-override" = overrides.title,
         });
-        const alloc = Application.default().allocator();
         const priv: *Private = self.private();
+        const alloc = core_app.alloc;
         priv.overrides = .{
             .command = if (overrides.command) |c| c.clone(alloc) catch null else null,
             .working_directory = if (overrides.working_directory) |wd| alloc.dupeZ(u8, wd) catch null else null,
@@ -1774,8 +1805,12 @@ pub const Surface = extern struct {
         // If our configuration is null then we get the configuration
         // from the application.
         if (priv.config == null) {
-            const app = Application.default();
-            priv.config = app.getConfig();
+            if (pending_embed_config) |config| {
+                priv.config = config.ref();
+            } else {
+                const app = Application.default();
+                priv.config = app.getConfig();
+            }
         }
 
         // Setup our input method state
diff --git a/src/apprt/gtk/embed.zig b/src/apprt/gtk/embed.zig
new file mode 100644
index 000000000..85e2f31cd
--- /dev/null
+++ b/src/apprt/gtk/embed.zig
@@ -0,0 +1,114 @@
+//! C ABI for embedding Ghostty's real GTK surface widget in a host GTK app.
+
+const std = @import("std");
+const gtk = @import("gtk");
+
+const CoreApp = @import("../../App.zig");
+const ApprtApp = @import("App.zig");
+const Application = @import("class/application.zig").Application;
+const Config = @import("class/config.zig").Config;
+const Surface = @import("class/surface.zig").Surface;
+const input = @import("../../input.zig");
+
+const log = std.log.scoped(.gtk_embed);
+
+pub const std_options: std.Options = .{};
+
+const EmbedApp = struct {
+    core_app: *CoreApp,
+    rt_app: *ApprtApp,
+};
+
+pub export fn ghostty_gtk_app_new() ?*EmbedApp {
+    return appNew() catch |err| {
+        log.err("error initializing embedded GTK app err={}", .{err});
+        return null;
+    };
+}
+
+fn appNew() !*EmbedApp {
+    const alloc = std.heap.c_allocator;
+
+    const core_app = try CoreApp.create(alloc);
+    errdefer core_app.destroy();
+
+    const rt_app = try alloc.create(ApprtApp);
+    errdefer alloc.destroy(rt_app);
+    try rt_app.init(core_app, .{});
+    errdefer rt_app.terminate();
+
+    Application.setEmbeddedDefault(rt_app.app);
+
+    const app = try alloc.create(EmbedApp);
+    app.* = .{
+        .core_app = core_app,
+        .rt_app = rt_app,
+    };
+    return app;
+}
+
+pub export fn ghostty_gtk_app_free(app: *EmbedApp) void {
+    const alloc = std.heap.c_allocator;
+    Application.setEmbeddedDefault(null);
+    app.rt_app.terminate();
+    app.rt_app.app.unref();
+    alloc.destroy(app.rt_app);
+    app.core_app.destroy();
+    alloc.destroy(app);
+}
+
+pub export fn ghostty_gtk_app_tick(app: *EmbedApp) void {
+    app.core_app.tick(app.rt_app) catch |err| {
+        log.err("error ticking embedded GTK app err={}", .{err});
+    };
+}
+
+pub export fn ghostty_gtk_surface_new(app: *EmbedApp) ?*Surface {
+    const config: *Config = app.rt_app.app.getConfig();
+    defer config.unref();
+
+    const surface = Surface.newWithCoreApp(app.core_app, config);
+    _ = surface.refSink();
+    return surface;
+}
+
+pub export fn ghostty_gtk_surface_widget(surface: *Surface) *gtk.Widget {
+    return surface.as(gtk.Widget);
+}
+
+pub export fn ghostty_gtk_surface_free(surface: *Surface) void {
+    surface.unref();
+}
+
+pub export fn ghostty_gtk_surface_redraw(surface: *Surface) void {
+    surface.redraw();
+}
+
+pub export fn ghostty_gtk_surface_title(surface: *Surface) ?[*:0]const u8 {
+    const title = surface.getEffectiveTitle() orelse return null;
+    return title.ptr;
+}
+
+pub export fn ghostty_gtk_surface_binding_action(
+    surface: *Surface,
+    action_ptr: [*]const u8,
+    action_len: usize,
+) bool {
+    const core_surface = surface.core() orelse return false;
+    const action_str = action_ptr[0..action_len];
+    const action = input.Binding.Action.parse(action_str) catch |err| {
+        log.err("error parsing binding action action={s} err={}", .{ action_str, err });
+        return false;
+    };
+
+    return core_surface.performBindingAction(action) catch |err| {
+        log.err("error performing binding action action={f} err={}", .{ action, err });
+        return false;
+    };
+}
+
+test {
+    _ = ghostty_gtk_app_new;
+    _ = ghostty_gtk_app_free;
+    _ = ghostty_gtk_app_tick;
+}
diff --git a/src/build/GhosttyLibGtk.zig b/src/build/GhosttyLibGtk.zig
new file mode 100644
index 000000000..9c883bd81
--- /dev/null
+++ b/src/build/GhosttyLibGtk.zig
@@ -0,0 +1,40 @@
+const GhosttyLibGtk = @This();
+
+const std = @import("std");
+const Config = @import("Config.zig");
+const SharedDeps = @import("SharedDeps.zig");
+
+lib: *std.Build.Step.Compile,
+install_step: *std.Build.Step.InstallArtifact,
+
+pub fn initShared(
+    b: *std.Build,
+    cfg: *const Config,
+    deps: *const SharedDeps,
+) !GhosttyLibGtk {
+    const lib = b.addLibrary(.{
+        .name = "ghostty-gtk",
+        .linkage = .dynamic,
+        .root_module = b.createModule(.{
+            .root_source_file = b.path("src/embed_gtk.zig"),
+            .target = cfg.target,
+            .optimize = cfg.optimize,
+            .strip = cfg.strip,
+            .omit_frame_pointer = cfg.strip,
+            .unwind_tables = if (cfg.strip) .none else .sync,
+        }),
+        .use_llvm = true,
+    });
+
+    _ = try deps.add(lib);
+
+    return .{
+        .lib = lib,
+        .install_step = b.addInstallArtifact(lib, .{}),
+    };
+}
+
+pub fn install(self: *const GhosttyLibGtk) void {
+    const b = self.install_step.step.owner;
+    b.getInstallStep().dependOn(&self.install_step.step);
+}
diff --git a/src/build/SharedDeps.zig b/src/build/SharedDeps.zig
index 70ff84b8c..0bb1fc87f 100644
--- a/src/build/SharedDeps.zig
+++ b/src/build/SharedDeps.zig
@@ -529,9 +529,8 @@ pub fn add(
         .freetype = true,
         .@"backend-metal" = target.result.os.tag.isDarwin(),
         .@"backend-osx" = target.result.os.tag == .macos,
-        // OpenGL3 backend should only be built on non-Apple targets.
-        // Apple platforms use Metal (and macOS may also use the OSX backend).
-        .@"backend-opengl3" = !target.result.os.tag.isDarwin(),
+        // Always enable opengl3 backend since GTK Apprt uses it
+        .@"backend-opengl3" = true,
     })) |dep| {
         step.root_module.addImport("dcimgui", dep.module("dcimgui"));
         step.linkLibrary(dep.artifact("dcimgui"));
@@ -580,8 +579,9 @@ pub fn add(
         }
     }
 
-    // If we're building an exe then we have additional dependencies.
-    if (step.kind != .lib) {
+    // If we're building an exe or embeddable GTK library then we have
+    // additional runtime dependencies.
+    if (step.kind != .lib or self.config.app_runtime == .gtk) {
         // We always statically compile glad
         step.addIncludePath(b.path("vendor/glad/include/"));
         step.addCSourceFile(.{
@@ -888,6 +888,7 @@ pub fn gtkNgDistResources(
         blueprint_exe.linkLibC();
         blueprint_exe.linkSystemLibrary2("gtk4", dynamic_link_opts);
         blueprint_exe.linkSystemLibrary2("libadwaita-1", dynamic_link_opts);
+        if (b.graph.host.result.os.tag == .macos) blueprint_exe.dead_strip_dylibs = true;
 
         for (gresource.blueprints) |bp| {
             const blueprint_run = b.addRunArtifact(blueprint_exe);
diff --git a/src/build/main.zig b/src/build/main.zig
index 1f36d375c..21e0cc423 100644
--- a/src/build/main.zig
+++ b/src/build/main.zig
@@ -14,6 +14,7 @@ pub const GhosttyExe = @import("GhosttyExe.zig");
 pub const GhosttyFrameData = @import("GhosttyFrameData.zig");
 pub const GhosttyLib = @import("GhosttyLib.zig");
 pub const GhosttyLibVt = @import("GhosttyLibVt.zig");
+pub const GhosttyLibGtk = @import("GhosttyLibGtk.zig");
 pub const GhosttyResources = @import("GhosttyResources.zig");
 pub const GhosttyI18n = @import("GhosttyI18n.zig");
 pub const GhosttyXcodebuild = @import("GhosttyXcodebuild.zig");
diff --git a/src/embed_gtk.zig b/src/embed_gtk.zig
new file mode 100644
index 000000000..440d8a6d5
--- /dev/null
+++ b/src/embed_gtk.zig
@@ -0,0 +1,3 @@
+comptime {
+    _ = @import("apprt/gtk/embed.zig");
+}
